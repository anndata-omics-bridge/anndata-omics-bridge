---
title: "SummarizedExperiment/AnnData Structure Specification for exploreDE"
author: 
  - "PL"
  - "WEW"
  - "Claude AI"
date: "2025-10-02"
format: 
  html: 
    toc: true
    toc-depth: 3
    number-sections: true
  pdf: 
    toc: true
    toc-depth: 3
    number-sections: true
    geometry: margin=1in
    documentclass: article
    fontsize: 11pt
    colorlinks: true
    tbl-cap-location: top
    fig-pos: 'H'
execute:
  eval: true
  echo: true
  warning: false
  message: false
editor: 
  markdown: 
    wrap: 72
---




# Use case

### Use Case: Harmonizing Omics Quantification Outputs for Downstream Analysis

Proteomics identification and quantification software — such as **Spectronaut**, **DIA-NN**, **alphaDIA**, **MaxQuant**, and **FragPipe** — produce results at various levels of biological aggregation, including *ion*, *peptidoform*, *protein*, *PTM site*, or *multisite* level.  
Similarly, **metabolomics** tools such as **MZmine** or **Compound Discoverer**, and **genomics** analysis pipelines, generate quantitative data tables with differing structures, column names, and metadata conventions.

For proteomics, the **`prolfquapp`** and **`prolfquappPTMreader`** R packages address this heterogeneity by providing specialized importers for each tool. They parse and normalize the diverse table structures, transforming them into standardized **LFQData** objects that enable robust differential expression analysis. However, changes in the upstream output formats can easily break compatibility, as `prolfquapp` depends on specific column naming and structure conventions.
Projects like **ProteoBench** also provide parsers for many proteomics quantification tools, and could be extended to export data directly into **AnnData**.

Once standardized, `prolfquapp` exports results in the **SummarizedExperiment** format at multiple biological levels (protein, peptide, and site). These differ in the structure and metadata, which the **exploreDE** application accommodates through dedicated code patches. 

Nonetheless, structural changes to the SummarizedExperiment schema can disrupt compatibility with visualization and downstream tools.

To summarize:

- different omics technologies rely on diverse software ecosystems, and our goal is to interconnect them with analysis and visualization tools in a scalable way. 
- It is unrealistic for every downstream application (for differential expression, enrichment, or visualization) to natively handle the idiosyncrasies of all upstream quantification tools and omics modalities. 
- Maintaining extensive mapping tables between software packages becomes complex and fragile.

Therefore, an **intermediate data format** is needed—one that is both:

- **flexible enough** to represent omics- and tool-specific features, 
- yet **simple enough** to be generated easily, in an preprocessing step. 

The guiding principle for this format is to minimize requirements imposed on tool developers when writing into this format, while maintaining semantic richness for downstream interoperability.


1. **Convert** raw quantification outputs into a multi-assay data container (e.g., *SummarizedExperiment* or *AnnData*).  
2. **Add conversion support** for B-Fabric quantification applications to ensure integration with the FGCZ ecosystem.  
3. **Run the DE application**, starting from the multi-assay container and appending differential expression results.  
4. **Run the GSEA/ORA application**, which adds functional enrichment results.  
5. **Visualize** integrated quantitative and functional data in applications such as *exploreDE*.

This modular architecture allows interoperability across tools, omics domains, and institutions, ensuring long-term sustainability and reproducibility of quantitative omics analyses.

![Omics data processing flow](docs/omics_data_flow.png)


## Contract: “Essential Data” for Tool Interop

To make multiple omics tools work on a common container without brittle per-exporter adapters/importers, each downstream tool (exploreDE, prolfquaDE) that operates on **AnnData** must publish an **essential set** of required data, **column decorations** and metdata (uns).

The goal is a **minimal, stable contract**: enough information to fully support the tool’s features, while **abstaining from non-essential requirements** that complicate AnnData creation.

Each tool also implements a Validator function, tool, script, which allows easily to check if the multi-assay container file will work.


**Principles**


1. **Essential-only**: Keep the required fields to the minimum needed for the tool’s core functionality. Avoid nice to have's. 
  - Some additional metadata, e.g.  additional sample annotation, etc. contrast, can be added when using a tool, therefore no need to add it to the anndata at the beginning.
  
2. **Decorate `obs`/`var` first**: Encode semantics using **column decorations** `<role>__<name>`.  
   - Use `uns` **only** when a concept cannot be expressed as column-level decorations (e.g., contrast definitions, global settings).  
3. **Tool-specific contracts**: Each tool (e.g., exploreDE, prolfqua DE) documents its **essential contract**. Upstream writers and importers target the contract—**not** tool-internal structures.  
   - validation funciton

---



## Data Container Mapping

The specification bridges two ecosystem formats:

- **SummarizedExperiment** (R/Bioconductor): `rowData`, `colData`, `assays`, `metadata`
- **AnnData** (Python/scanpy): `var`, `obs`, `X`, `uns`

| Component | SummarizedExperiment | AnnData | Description |
|-----------|---------------------|---------|-------------|
| Features  | `rowData`           | `var/varm`   | annotation of Variables/ Features (Gene/protein/Metabolite annotations and DE results) |
| Samples   | `colData`           | `obs/obsm`   | annotation of Observations (Sample metadata and experimental factors) |
| Counts    | `assays`            | `X`/`layers`     | Raw/normalized/transformed abundance matrices |
| Other     | `metadata`          | `uns`   | Unstructured metadata (pathways, settings) |




# Assays/ Layers/ X (Counts / Abundances/ Intensities)

- Abundance matrices stored in `assays()`. Rows = features, columns = samples.
- Abundance matrices stored in `layers` in AnnData, Rows = obs, columns = vars.

Assay names should be descriptive of the data type or transformation applied (e.g., `counts`, `tpm`, `vst`, `log2_tpm`).

The distribution of the data (exponential, poisson, normal) is **not encoded in the assay name**. Instead, tools can determine the sampling distribution statistically when needed for visualization or analysis.

**Important:** Annotate in `metadata(se)` which assay was used for each DE test.

**Code example:**

```{r assays-example}
library(SummarizedExperiment)
# Define gene IDs (used throughout)
gene_ids <- c("GENE001", "GENE002", "GENE003")
sample_ids <- c("sample1", "sample2", "sample3", "sample4")

# Create count matrices
counts <- matrix(
  c(100, 150, 200, 180,
    50, 60, 20, 25,
    1000, 1100, 1050, 1020),
  nrow = 3, ncol = 4,
  dimnames = list(gene_ids, sample_ids)
)

# Transformed abundance (log2)
vst <- log2(counts + 1)

# Normalized abundance (TPM - example)
tpm <- counts / colSums(counts) * 1e6

se <- SummarizedExperiment(
  assays = list(
    counts = counts,
    vst = vst,
    tpm = tpm
  ))

```


| Assay Type | Description | Examples |
|:-----------|:------------|:---------|
| Numeric abundance | Raw, normalized, or transformed abundance data | `counts`, `tpm`, `fpkm`, `intensities` |
| Transformed abundance | Log-transformed or variance-stabilized data | `log2_tpm`, `vst`, `voom`, `vsn` |
| Metadata matrices | Feature-specific annotations per sample | `nr_peptides`, `stdev`, `retention_time` |

**Common Assay Name Examples**

| Assay Name | Description |
|:------------|:-------------|
| `counts` | Raw read counts (RNA-seq) |
| `tpm` | Transcripts per million |
| `fpkm` | Fragments per kilobase per million |
| `intensities` | Raw MS intensities (proteomics/metabolomics) |
| `log2_tpm` | Log2-transformed TPM |
| `vst` | Variance stabilizing transformation (DESeq2) |
| `voom` | Voom transformation (limma) |
| `vsn` | Variance stabilizing normalization |
| `nr_peptides` | Number of peptides quantified per protein per sample |
| `stdev` | Standard deviation of technical replicates  |
| `retention_time` | Chromatographic retention time (metabolomics) |

## Determining Data Distribution

Tools can determine the sampling distribution of assay data statistically using moment-based classification. This approach uses skewness and kurtosis to classify data and suggest appropriate visualization scales.

**Code example:**

```{r classify-scale-function}
library(moments)

classify_scale <- function(x) {
  x <- x[is.finite(x)]
  sk <- skewness(x)
  ku <- kurtosis(x)

  # Default
  scale <- "linear"
  category <- "normal_like"

  # Decision rules
  # - If data are strictly positive AND right-skewed → log scale
  # - If skewness > 0.8 OR kurtosis > 5 → "exp_like"
  if (all(x > 0) && (sk > 0.8 || ku > 5)) {
    scale <- "log"
    category <- "exp_like"
  }

  list(skewness = sk, kurtosis = ku,
       classification = category,
       suggested_x_scale = scale)
}

# Example usage with the assays created above
classify_scale(as.vector(assays(se)$counts))
classify_scale(as.vector(assays(se)$vst))
```

This approach allows visualization tools to automatically select appropriate scales (linear vs. log) and statistical methods based on the empirical distribution of the data, without requiring distribution information to be encoded in assay names.

---

# Var/Varm  (rawData/Features)

Feature annotations and differential expression results stored in `var` or `rowData()`.

We can store genes, proteins, peptides, metabolites etc. in the `var` container.
To identify which type of data is stored we can use the following:

- anndata : `data.var.index.name = 'gene_id'`
- SummarizedExperiment : `rawData(se)[[`gene_id`]] <- DataFrame()`

## Feature Annotation Table

**Single table shared across all DE tests in the SummarizedExperiment.**

All columns can be used for filtering and subsetting. Column types (string, integer, numeric) determine available filter operations.


| Column Name |  Type | Required? | Distinct? | Description |
|-------------|------|-----------|-------------|
| `<rowname>` | string | Yes | Yes | Row identifier matching rownames of all assays (e.g., gene ID, protein ID, Ensembl ID) |
| `description` | string | Yes | Yes? | Free-text description (searchable) |
| `label_gene_symbol` | string | No | No | Human-readable gene symbol |
| `label_entrez_id` | string | No | No | NCBI Entrez Gene ID |
| `label_ensembl_gene_id` | string | No | No | Ensembl gene identifier |
| `label_uniprot_id` | string | No | No | UniProt accession |
| `label_flydb_id` | string | No | No | FlyBase identifier |
| `label_nr_peptides` | numeric | No | No | Number of peptides quantifying this protein |
| `label_global_base_mean` | numeric | No | No | Mean abundance across all samples |
| `label_assignment_method` | string | No | No | Annotation method (MS1, MS2, manual) |


The description column is used to store free-text description of the feature e.g. "Amyloid-beta precursor protein", "BRCA1 DNA repair", "EGFR receptor", or for metabolomics compounds "Caffeine", "Theobromine", "Theophylline", etc. this information is primarily used for searching using names used by biologists.

**Code example:**

```{r feature-annotation-example}
#| warning: false
#| message: false

# Define gene IDs (used throughout)
gene_ids <- c("GENE001", "GENE002", "GENE003")

# Feature annotation table (shared across all DE tests)
feature_annotation <- DataFrame(
  row.names = gene_ids,
  description = c("Tumor protein p53", "BRCA1 DNA repair", "EGFR receptor"),
  label_gene_symbol = c("TP53", "BRCA1", "EGFR"),
  label_entrez_id = c("7157", "672", "1956"),
  label_global_base_mean = c(150.5, 42.3, 1050.2)
)

rowData(se)[["gene_id"]] <- feature_annotation

rowData(se)

```

## Hierarchical Data Columns (prefix: `hierarchy_`)

For proteomics and other hierarchical omics data, use `hierarchy_` columns to encode multi-level relationships. Individual hierarchy columns do not need to be distinct, but **all hierarchy columns combined must ensure distinct rows** (matching the `<rowname>` identifiers).


- Each `hierarchy_*` column alone may contain duplicates
- The **combination** of all `hierarchy_*` columns must uniquely identify each row
- This allows rollup/aggregation from lower to higher levels (e.g., peptide -> protein)
- The hierarchy levels should be ordered left-to-right from coarsest to finest granularity

**Representing Hierarchies**

Store hierarchy data in columns, define structure in metadata.

```r
# Data: hierarchy_* columns in rowData
var <- DataFrame(row.names = "PEPTIDE_001", 
hierarchy_protein = "P53_HUMAN",
hierarchy_peptide = "SVTEQGAELSNEER",
hierarchy_peptidoform = "SVTEQGAELSNEER[Phospho]",
hierarchy_ion = "762.3442_2")

# Structure: metadata defines levels and relationships
metadata(se)$hierarchy_definition <- c("protein", "peptide", "peptidoform", "ion")
```



**Peptide-level hierarchy:**

| Column Name | Type | Description | Example Values |
|-------------|------|-------------|----------------|
| `<rowname>` | string | Distinct row identifier | "PEPTIDE_001", "PEPTIDE_002", "PEPTIDE_003" |
| `hierarchy_protein` | string | Protein identifier | "P53_HUMAN", "P53_HUMAN", "P53_HUMAN" |
| `hierarchy_peptide` | string | Peptide sequence | "SVTEQGAELSNEER", "ALPNNTSSSPQPK", "CSDSDGLAPPQHLIR" |


**Example**

```{r peptide-hierarchy-example}
# Peptide-level proteomics feature annotation
peptide_annotation <- DataFrame(
  row.names = paste0("PEPTIDE_", 1:5),
  hierarchy_protein = c("P53_HUMAN", "P53_HUMAN", "P53_HUMAN", 
                        "BRCA1_HUMAN", "BRCA1_HUMAN"),
  hierarchy_peptide = c("SVTEQGAELSNEER", "ALPNNTSSSPQPK", 
                        "CSDSDGLAPPQHLIR", "TLNIFGSTR", "QETSVFDCNK"),
  description = c("Tumor suppressor p53", "Tumor suppressor p53", 
                  "Tumor suppressor p53", "BRCA1 DNA repair", 
                  "BRCA1 DNA repair"),
  label_gene_symbol = c("TP53", "TP53", "TP53", "BRCA1", "BRCA1"),
  label_uniprot_id = c("P04637", "P04637", "P04637", "P38398", "P38398"),
  label_nr_peptides_per_protein = c(3, 3, 3, 2, 2)
)

# Check that hierarchy columns combined are distinct
nrow(peptide_annotation) == nrow(unique(peptide_annotation[, 
  c("hierarchy_protein", "hierarchy_peptide")]))

metadata(se)$hierarchy_definition <- c("protein", "peptide")
```


**Peptidoform and ion-level hierarchy:**

| Column Name | Type | Description | Example Values |
|-------------|------|-------------|----------------|
| `<rowname>` | string | Distinct row identifier | "ION_001", "ION_002" |
| `hierarchy_protein` | string | Protein identifier | "P53_HUMAN" |
| `hierarchy_peptide` | string | Peptide sequence | "SVTEQGAELSNEER" |
| `hierarchy_peptidoform` | string | Modified peptide sequence | "SVTEQGAELSNEER[Phospho]" |
| `hierarchy_ion` | string | Precursor ion (m/z, charge) | "762.3442_2", "508.5628_3" |



**Phosphoproteomics - site-level hierarchy:**

For phosphorylation site analysis (single site):

| Column Name | Type | Description | Example Values |
|-------------|------|-------------|----------------|
| `<rowname>` | string | Distinct row identifier | "PHOSPHO_001", "PHOSPHO_002", "PHOSPHO_003" |
| `hierarchy_protein` | string | Protein identifier | "P53_HUMAN", "P53_HUMAN", "TAU_HUMAN" |
| `hierarchy_site` | string | Single phosphorylation site | "S15", "S20", "S199" |

**Phosphoproteomics - site with multiplicity:**

When tracking phosphorylation multiplicity (multiple phosphorylation events on the same peptide):

| Column Name | Type | Description | Example Values |
|-------------|------|-------------|----------------|
| `<rowname>` | string | Distinct row identifier | "PHOSPHO_001", "PHOSPHO_002", "PHOSPHO_003", "PHOSPHO_004" |
| `hierarchy_protein` | string | Protein identifier | "TAU_HUMAN", "TAU_HUMAN", "TAU_HUMAN", "TAU_HUMAN" |
| `hierarchy_site` | string | Single phosphorylation site | "S199", "S199", "S199", "S396" |
| `hierarchy_multiplicity` | integer | Number of phosphorylations on peptide | 1, 2, 3, 2 |


Example:
```{r phospho-multiplicity-example}
# Phosphosite-level data with multiplicity
# Each row represents a specific site at a specific multiplicity level
phospho_annotation <- DataFrame(
  row.names = paste0("PHOSPHO_", 1:5),
  hierarchy_protein = c("TAU_HUMAN", "TAU_HUMAN", "TAU_HUMAN", 
                        "TAU_HUMAN", "TAU_HUMAN"),
  hierarchy_site = c("S199", "S199", "S199", "S202", "S396"),
  hierarchy_multiplicity = c(1, 2, 3, 1, 2),
  description = c("Tau S199 - single phosphorylation on peptide", 
                  "Tau S199 - double phosphorylation on peptide",
                  "Tau S199 - triple phosphorylation on peptide",
                  "Tau S202 - single phosphorylation on peptide",
                  "Tau S396 - double phosphorylation on peptide"),
  label_gene_symbol = c("MAPT", "MAPT", "MAPT", "MAPT", "MAPT"),
  label_uniprot_id = c("P10636", "P10636", "P10636", "P10636", "P10636")
)

# Check that hierarchy columns combined are distinct
nrow(phospho_annotation) == nrow(unique(phospho_annotation[, 
  c("hierarchy_protein", "hierarchy_site", "hierarchy_multiplicity")]))

metadata(se)$hierarchy_definition <- c("protein", "site", "multiplicity")
```


---

## Differential Expression Tables (Varm)

**One set of columns per DE test performed.**

Each DE test is stored as a separate DataFrame in the `rowData()` list, accessed by a distinct name.

The DE table name format is: `DE_<name_of_contrast>` where `<name_of_contrast>` is a distinct identifier for this comparison.

**Code example:**

```{r de-results-example}
# DE results table (one per contrast)
de_treated_vs_control <- DataFrame(
  row.names = gene_ids,
  effect_log2fc = c(2.5, -1.8, 0.3),
  score_p_value = c(0.001, 0.005, 0.45),
  score_fdr = c(0.01, 0.03, 0.67),
  label_de_base_mean = c(155.2, 38.1, 1048.9),
  label_t_statistic = c(8.2, -5.1, 0.8)
)

# Access DE results from a SummarizedExperiment
rowData(se)[["DE_treated_vs_control"]] <- de_treated_vs_control

de_results <- rowData(se)[["DE_treated_vs_control"]]
```

### Required DE Columns

| Column Name | Type | Distinct? | Alternative Names |Description |
|-------------|------|-----------|-------------------|-------------|
| `<rowname>` | string | Yes |                      | Row identifier matching rownames of feature annotation table and all assays |
| `effect_*` | numeric | No |  `log2Ratio`, `log2FC`, `lfc` | Log2 fold change or other effect size |
| `score_*` | numeric | No |  `score_pvalue`, `score_adjusted_p_val`, `score_q_value` | Raw p-value, adjusted p-value / FDR / q-value |
| `label_*` | numeric/string | No |  `label_model_name`, `label_de_base_mean`, `label_t_statistic`, `label_n_missing`, `label_ci_upper`, `label_ci_lower` | Additional statistics or metadata, either numeric or string |

- effect_ : Used for volcano plot x-axis.
- score_ : Used for volcano plot y-axis. At least one score column required.
- label_ : Additional statistics or metadata, either numeric or string.


**Note for MA plots:**

- DESeq2: `rowData(metadata(se)$nativeResult$dds)$baseMean`
- edgeR: `metadata(se)$nativeResult$AveLogCPM`


For each DE test, the following information should be stored in the metadata/uns:

`metadata(se)$de_tests[[<name_of_contrast>]]`:

| Field | Required? | Type | Description | Example |
|-------|------|-------------|---------|---------|
| `assay_used` | Yes | string | Which assay was used for DE | `"vsn"` |
| `factor_used` | Yes | character vector | Which `factor_*` column(s) from `colData` were used | `c("factor_treatment", "factor_batch")` |
| `contrast_formula` | No | string | Exact mathematical formula | `"factor_treatment_B - factor_treatment_control"` |
| `model` | No | string | Statistical model used (e.g., "DESeq2", "limma", "prolfqua") | `"DESeq2"` |

**Example:**

```{r de-metadata-example}
metadata(se)$de_tests <- list(
  treatmentB_vs_control = list(
    assay_used = "vst",
    factor_used = c("factor_condition", "factor_batch"),
    contrast_formula = paste("factor_condition_treatmentB", 
                             "- factor_condition_control"),
    model = "DESeq2"
  ),
  timepoint8h_vs_0h = list(
    assay_used = "vst",
    factor_used = "factor_timepoint",
    contrast_formula = paste("factor_timepoint_8", 
                             "- factor_timepoint_0"),
    model = "DESeq2"
  )
)

# Access the corresponding DE results
de_table <- rowData(se)[["DE_treatmentB_vs_control"]]
de_metadata <- metadata(se)$de_tests$treatmentB_vs_control
```

---

# Samples/ obs / colData

Sample metadata and experimental design information stored in `colData()`.

TODO: How to accomodate different sample types : e.g. QC samples, and differentiate them from biological samples etc.

| Column Name | Type | Required? | Distinct? | Description |
|-------------|:------|:-----------|:----------|:-------------|
| `<rowname>` | string | Yes | Yes |  Sample identifier matching colnames of all assays (typically same as `sample_id`) |
| `sample_label` | string | No | Yes | Human-readable sample name |
| `bfabric_sample_id` | string | No | No | B-Fabric database ID |
| `factor_*` | categorical or numeric | No | no | Experimental factors (condition, sex, treatment, batch, timepoint, genotype) |
| `organism` | string | No | No | Species name |
| `feature_level` | string | No | No | Feature type (isoform/gene, peptide/protein) |
| `label_*` | string or numeric | No | No | Sample-level annotations (read counts, creatinine concentration, etc.) |


**Factors:** Categorical or continuous covariates used in experimental design.

- `factor_condition` (e.g., "control", "treated")
- `factor_sex` (e.g., "M", "F")
- `factor_batch` (e.g., "batch1", "batch2")
- `factor_timepoint` (numeric, e.g., 0, 2, 4, 8 hours)
- `factor_genotype` (e.g., "WT", "KO")

**Code example:**

```{r coldata-example}
# Define sample IDs (used throughout)
sample_ids <- c("sample1", "sample2", "sample3", "sample4")

# Sample metadata
col_data <- DataFrame(
  row.names = sample_ids,
  sample_id = sample_ids,
  sample_label = c("Ctrl_1", "Ctrl_2", "Treat_1", "Treat_2"),
  factor_condition = factor(c("control", "control", "treated", 
                              "treated")),
  factor_batch = factor(c("A", "B", "A", "B")),
  organism = "Homo sapiens",
  feature_level = "gene"
)
```


# Metadata / uns

Unstructured metadata stored in `metadata(se)`

- to represent differential expression analysis annotations not stored in the DE tables (see section Differential Expression Tables (Varm)).
- to represent hierarchical data structures not stored in the rowData tables (see section Hierarchical Data Columns).


## TODO : Pathway Analyses Results

Pathway enrichment and gene set analysis results.


Store native objects for compatibility:

- `gseaResult` objects (Gene Set Enrichment Analysis)
- `enrichResult` objects (Over-Representation Analysis)

### Alternative: Data Frame Format **to be discussed**

If native objects are not used, store pathway results as data frames with the following columns:

| Column | Type | Description |
|--------|------|-------------|
| `category` | string | Pathway database (GO, KEGG, Reactome, etc.) |
| `id` | string | Pathway identifier (GO:0008150, hsa04110, etc.) |
| `description` | string | Pathway name/description |
| `n_genes_mapped` | integer | Number of genes from input mapping to this pathway |
| `n_genes_in_set` | integer | Total genes in pathway |
| `enrichment_score` | numeric | Enrichment score (varies by method) |
| `fdr` | numeric | False discovery rate |
| `method` | string | Analysis method (ORA, GSEA, etc.) |
| `feature_ids` | list | List of mapped feature IDs (matching rowData rownames) |
| `feature_labels` | list | List of human-readable feature labels |

**TODO:** Review ClusterProfiler's `enrichResult` structure and STRING-DB web API for compatibility.

---

# Complete Example

## Full Constructor

This section assembles all the code snippets from previous sections into a complete, executable example.

```{r complete-constructor}
#| eval: true
#| warning: false
#| message: false
library(SummarizedExperiment)

# ============================================================================
# 1. Define identifiers
# ============================================================================
gene_ids <- c("GENE001", "GENE002", "GENE003")
sample_ids <- c("sample1", "sample2", "sample3", "sample4")

# ============================================================================
# 2. Create assays (Section 1: Assays)
# ============================================================================
counts <- matrix(
  c(100, 150, 200, 180,
    50, 60, 20, 25,
    1000, 1100, 1050, 1020),
  nrow = 3, ncol = 4,
  dimnames = list(gene_ids, sample_ids)
)

# Transformed abundance (log2)
vst <- log2(counts + 1)

# Normalized abundance (TPM - example)
tpm <- counts / colSums(counts) * 1e6

# ============================================================================
# 3. Create colData (Section 3: Samples/obs)
# ============================================================================
col_data <- DataFrame(
  row.names = sample_ids,
  sample_id = sample_ids,
  sample_label = c("Ctrl_1", "Ctrl_2", "Treat_1", "Treat_2"),
  factor_condition = factor(c("control", "control", "treated", 
                              "treated")),
  factor_batch = factor(c("A", "B", "A", "B")),
  organism = "Homo sapiens",
  feature_level = "gene"
)

# ============================================================================
# 4. Create rowData (Section 2: Var/Features)
# ============================================================================
# Feature annotation table (shared across all DE tests)
feature_annotation <- DataFrame(
  row.names = gene_ids,
  description = c("Tumor protein p53", "BRCA1 DNA repair", "EGFR receptor"),
  label_gene_symbol = c("TP53", "BRCA1", "EGFR"),
  label_entrez_id = c("7157", "672", "1956"),
  label_global_base_mean = c(150.5, 42.3, 1050.2)
)

# DE results table (one per contrast)
de_treated_vs_control <- DataFrame(
  row.names = gene_ids,
  effect_log2fc = c(2.5, -1.8, 0.3),
  score_p_value = c(0.001, 0.005, 0.45),
  score_fdr = c(0.01, 0.03, 0.67),
  label_de_base_mean = c(155.2, 38.1, 1048.9),
  label_t_statistic = c(8.2, -5.1, 0.8)
)

# ============================================================================
# 5. Create metadata (Section 4: Metadata/uns)
# ============================================================================
metadata_list <- list(
  de_tests = list(
    treated_vs_control = list(
      assay_used = "vst",
      factor_used = "factor_condition",
      contrast_formula = paste("factor_condition_treated", 
                               "- factor_condition_control"),
      model = "DESeq2"
    )
  ),
  hierarchy_definition = c("gene_id"),
  ref_build = "GRCh38"
)

# ============================================================================
# 6. Construct SummarizedExperiment
# ============================================================================
se <- SummarizedExperiment(
  assays = list(
    counts = counts,
    vst = vst,
    tpm = tpm
  ),
  rowData = list(
    gene_id = feature_annotation,
    DE_treated_vs_control = de_treated_vs_control
  ),
  colData = col_data,
  metadata = metadata_list
)

# ============================================================================
# 7. Verify and access the data
# ============================================================================
# Show object summary
se

# Access different components
assays(se)$counts[1:3, 1:4]
assays(se)$vst[1:3, 1:4]
colData(se)[1:4, ]
rowData(se)[["gene_id"]][1:3, ]
rowData(se)[["DE_treated_vs_control"]][1:3, ]
metadata(se)$de_tests$treated_vs_control
```

---

# TODO : Dimensionality Reduction Results

## Storage of PCA, UMAP, and Other Embeddings

Dimensionality reduction analyses (PCA, UMAP, t-SNE) are computationally intensive and should be cached to avoid re-computation.

### Use Case: PCA for Missing Value Imputation

**Problem:** PCA-based imputation is computationally expensive, especially with:
- Large feature sets (genes, proteins)
- Multiple missing values
- Iterative imputation algorithms (e.g., NIPALS, EM-based)


---

# Validation

## Validating SummarizedExperiment Objects

The `validate_summarized_experiment()` function checks if a SummarizedExperiment object conforms to this specification.

The validator performs the following checks:

1. **Basic Structure**: Confirms object is a SummarizedExperiment
2. **Required Components**: Verifies presence of assays, rowData, colData, metadata
3. **Assays**: 
   - Checks for at least one numeric assay
   - Ensures consistent dimensions across all assays
4. **colData**:
   - Verifies row names are present and distinct
   - Checks for `factor_*` columns
   - Validates column naming conventions
5. **rowData**:
   - Looks for feature annotation table (e.g., `gene_id`, `protein_id`, `peptide_id`)
   - Identifies DE tables (`DE_*`)
   - Validates required columns (description, `effect_*`, `score_*`)
   - Checks naming conventions (`label_*`, `hierarchy_*`, `effect_*`, `score_*`)
6. **Metadata**:
   - Validates DE test metadata structure (`metadata(se)$de_tests`)
   - Checks required fields (`assay_used`, `factor_used`)
   - Validates optional fields (`contrast_formula`, `model`)
7. **Consistency**:
   - Ensures row/column dimensions match across components
   - Validates DE table names match metadata keys
   - Verifies assay names referenced in metadata exist

## Validation Result Structure

The validation function returns a list with:

- `overall_status`: "PASS" or "FAIL"
- `errors`: Critical issues preventing use
- `warnings`: Non-critical issues or recommendations
- `checks`: Detailed per-check results with status and messages


## Usage Examples

```{r validation-source}
#| eval: true
#| warning: false
#| message: false
source("validate_summarized_experiment.R")
validate_summarized_experiment(se, verbose = TRUE)
```


```{r validation-example}
#| eval: true
#| warning: false
#| message: false
# Create an example SummarizedExperiment
se_example <- create_example_se()

# Validate the object
validation_result <- validate_summarized_experiment(se_example, verbose = TRUE)

# Check overall status
if (validation_result$overall_status == "PASS") {
  cat("\n[PASS] SummarizedExperiment is valid!\n")
} else {
  cat("\n[FAIL] Validation failed\n")
  cat("\nErrors:\n")
  print(validation_result$errors)
}
```




---

# Version History

| Date | Version | Changes |
|------|---------|---------|
| 2025-10-02 | 1.0 | Initial specification document (restructured) |

---

# References

- [SummarizedExperiment Bioconductor package](https://bioconductor.org/packages/SummarizedExperiment/)
- [AnnData documentation](https://anndata.readthedocs.io/)
- [ClusterProfiler enrichResult objects](https://yulab-smu.top/biomedical-knowledge-mining-book/)
- [STRING-DB API](https://string-db.org/help/api/)

